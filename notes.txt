<C-v> u00B2  : add ²

:Ntree [directory] : set directroy as current in netrw

= : apply autoindent to block

=G :  apply autoindent to end of file

u : undo

set past  : past from clipboard

% : create file in netrw
d : create directory in netrw
D : delete in netrw
R : rename in netrw

<C-f> : page forward
<C-b> : page back
<C-d> : down half a page
<C-u> : up half a page
H : High
M : Middle
L : Lower
<C-y> : scroll up
<C-e> : scroll down

/\c<search> : case insensitive search

* : search current word
# : serach current word backwards

<C-g> : preview search
<C-t> : preview search backwards

~ : change caps

# autocomplete
<C-n> :  autocomplete 
<C-x> <C-n> : only from open file       
<C-x> <C-f> : autocomplete file path 

# add, sub number
<C-a> add
<C-x> subtract

# tabs
:tabe
gt : tabnext
gT : tabprev

# auto complte
Ctrl-n   :      auto complet in insert mode
Ctrl-x Ctrl-n  :  only in current file
Ctrl-x Ctrl-f  : auto complet file paths


# scrolling
e : end
y : up

_g : last none space        

# mark
m<register>  : set mark
`<register>  : go to mark
